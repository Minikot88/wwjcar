# Prefer SSH tunnel. If direct DBeaver access is required, replace TRUSTED_PUBLIC_IP.
# Do not use 0.0.0.0/0.
#
# TYPE  DATABASE  USER        ADDRESS              METHOD
host    wwjcar    wwjcar_app  TRUSTED_PUBLIC_IP/32  scram-sha-256
